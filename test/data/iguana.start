#!/bin/sh

eval `scramv1 runtime -sh`

export LD_LIBRARY_PATH=`echo $LD_LIBRARY_PATH | sed -e 's;/cms/software/${SCRAM_ARCH}/external/gcc/3.2.3/lib:;;'`

iguana --iguana-session 'Vis Example--NTuple browser'

#iguana --dqm-collector-hostname localhost --dqm-collector-port 9090 --iguana-session 'Vis Example--NTuple browser'

